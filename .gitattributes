# Normalise line endings so the repo stays diff-clean across Windows/Linux.
* text=auto eol=lf

# Windows shell scripts keep CRLF.
*.ps1 text eol=crlf

# Binary-ish
*.png binary
*.jpg binary
*.pdf binary
